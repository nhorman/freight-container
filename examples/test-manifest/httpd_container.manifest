

# Pull in the base manifest
inherit = "./examples/test-manifest/base.manifest";


# Add the stuff we need to set up a web server
manifest = (
	"passwd",
	"policycoreutils",
	"selinux-policy-targeted",
	"httpd"
);

packaging = {
	name = "httpd_container";
	version = "1.0";
	release = "1";
	summary = "example container";
	license = "GPLv2";
	author = "Neil Horman <root@localhost.com>";
};

yum_opts = {
	releasever = "21";
};


